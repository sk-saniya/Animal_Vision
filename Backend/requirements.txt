""""#requirements.txt
torch>=2.0.0
torchvision>=0.15.0
tqdm>=4.65.0
Pillow>=9.0.0
ftfy>=6.1.0
regex>=2023.0.0

# OpenAI CLIP — install directly from GitHub (not on PyPI)
# Run this separately:
 git+https://github.com/openai/CLIP.git
 """"'

# ──────────────────────────────────────────────
# requirements.txt
# Animal Classification — CNN + CLIP (Option 2)
# ──────────────────────────────────────────────

torch>=2.0.0
torchvision>=0.15.0
tqdm>=4.65.0
Pillow>=9.0.0
ftfy>=6.1.0
regex>=2023.0.0

# ⚠️  OpenAI CLIP is NOT on PyPI — install separately:
 git+https://github.com/openai/CLIP.git