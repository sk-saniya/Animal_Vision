# ──────────────────────────────────────────────
# requirements.txt — Animal Vision Backend
# ──────────────────────────────────────────────

# Core ML
torch>=2.0.0
torchvision>=0.15.0

# Image handling
Pillow>=9.0.0

# Utilities
tqdm>=4.65.0
ftfy>=6.1.0
regex>=2023.0.0

# Web framework
flask>=2.3.0
flask-cors>=4.0.0

# YOLO
ultralytics>=8.0.0

# OpenAI CLIP (not on PyPI — installed via git)
git+https://github.com/openai/CLIP.git